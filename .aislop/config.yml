# SPDX-FileCopyrightText: 2026 Andrew Grimberg <tykeal@bardicgrove.org>
# SPDX-License-Identifier: Apache-2.0

---

version: 1

# aislop 0.12.x supports precise false-positive handling via inline
# `aislop-ignore-*` directives in source. Keep project-local config here.

# Local pre-commit fails if a scoreable tree drops below a perfect 100.
# This tree is not scored yet because it has no scoreable files; the
# threshold takes effect once Python source lands.
ci:
  failBelow: 100
